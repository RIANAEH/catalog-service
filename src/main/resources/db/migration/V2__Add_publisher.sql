-- ===========================================
-- [클라우드 네이티브 스프링 - Flyway 마이그레이션]
-- V2__Add_publisher.sql: publisher 컬럼 추가
--
-- [실무 조언 - 무중단 배포를 위한 마이그레이션]
-- 1. 새 컬럼은 NULL 허용 또는 기본값 설정
-- 2. NOT NULL 제약은 데이터 채운 후 별도 마이그레이션으로 추가
-- 3. 컬럼 삭제는 코드 배포 후 별도 마이그레이션으로 처리
-- ===========================================

ALTER TABLE book ADD COLUMN publisher VARCHAR(255);
